name: Byg usigneret IPA

# Web udrulles direkte til Netlify, saa IPA'en bygges kun naar der
# bedes om det. Ellers koerer tre builds i koe for tre smaa rettelser.
on:
  workflow_dispatch:

jobs:
  ipa:
    runs-on: macos-15
    timeout-minutes: 60

    strategy:
      fail-fast: false
      matrix:
        xcode: ['26.2.0']

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Vaelg Xcode ${{ matrix.xcode }}
        run: |
          sudo xcode-select -s "/Applications/Xcode_${{ matrix.xcode }}.app"
          xcodebuild -version

      - name: Byg
        run: bash ./build-ipa.sh

      - uses: actions/upload-artifact@v4
        with:
          name: mo-madhakka-ipa-xcode-${{ matrix.xcode }}
          path: build/app.ipa
          retention-days: 30
          if-no-files-found: error

      - name: Byg development client
        run: bash ./build-devclient.sh

      - uses: actions/upload-artifact@v4
        with:
          name: mo-madhakka-devclient
          path: build/dev.ipa
          retention-days: 30
          if-no-files-found: error
